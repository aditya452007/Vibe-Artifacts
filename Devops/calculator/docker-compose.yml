services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    command: gunicorn -c gunicorn.conf.py app:app

  frontend:
    build:
      context: ./frontend
    ports:
      - "8081:80"
